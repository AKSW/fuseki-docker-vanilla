# Single source of truth for versions.
# Keep lines plain KEY=VALUE with shell-style ${VAR} refs:
# this syntax works both for GNU make (include) and sh (source).
FUSEKI_VERSION=6.2.0
IMAGE_TAG=${FUSEKI_VERSION}-2
